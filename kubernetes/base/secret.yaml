apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: default
type: Opaque
stringData:
  # Placeholder value — in production this is managed by External Secrets Operator
  # pulling from AWS Secrets Manager. Never commit a real value here.
  secret_key: "placeholder-replace-via-external-secrets"
