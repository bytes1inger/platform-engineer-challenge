apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps

resources:
  - ../../base/sample-app

patches:
  - patch: |-
      - op: replace
        path: /metadata/labels/cluster
        value: onprem
    target:
      kind: Deployment
      name: sample-app
